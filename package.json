{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "webhook-notify",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.0.0"
  },
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/webhook-notify.js --target=bun --format esm --external @opencode-ai/plugin",
    "test": "bun test"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.15.10"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
